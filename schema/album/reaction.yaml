type: object
properties:
  _id:
    type: string
    nullable: true
    format: uuid
    description: reaction id.
  status:
    type: string
    nullable: true
    description: reaction status of the self.
    $ref: "#/components/enums/reaction"
  $ref: "/schema/album/reactionItems.yaml"
