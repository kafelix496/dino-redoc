allOf:
  - type: object
    properties:
      key:
        $ref: "/schema/album/assetKey.yaml"
      extension:
        type: string
        description: file extension.
  - $ref: "/schema/shared/time.yaml"
