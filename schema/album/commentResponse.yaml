allOf:
  - type: object
    properties:
      _id:
        type: string
        format: uuid
        description: comment id.
      content:
        $ref: "/schema/album/commentContent.yaml"
      parent:
        type: string
        description: target parent that has comment.
        enum:
          - "album.posts"
          - "album.assets"
      parentId:
        type: string
        format: uuid
        description: target parent id.
  - $ref: "/schema/shared/time.yaml"
